USE codeup_test_db;

CREATE TABLE albums (
    column1_name data_type,
    column2_name data_type,
    id int(10),
    artist varchar(50),
    name varchar (25),
    release_date(4),
    sales float(1000),
    genre varchar(20)

    SHOW CREATE TABLE quotes;
);